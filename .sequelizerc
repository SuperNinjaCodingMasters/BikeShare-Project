const path = require('path');

module.exports = {
    'config': path.resolve('./src/server/config', 'config.json'),
    'migrations-path': path.resolve('./src/server/config', 'migrations'),
    'models-path': path.resolve('./src/server', 'models'),
    'seeders-path': path.resolve('./src/server/config', 'seeders')
}